version: "3"

services:
    web-server:
        image: pierrezemb/gostatic
        ports:
            - 8043:8043
        volumes:
            - ./http:/srv/http
        networks:
            -  proxy_net

networks:
    proxy_net:
        external: true
